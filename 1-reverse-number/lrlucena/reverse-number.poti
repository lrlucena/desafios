# Linguagem Potigol (potigol.github.io)
entrada = leia_texto

só_números = entrada.selecione(c => c>='0' e c<='9')

resposta = se entrada == só_números então
  entrada.injete("")((as, a) => a + as)
senão
  "erro"
fim

escreva resposta

